
# Soundcloud Shortcodes

- To embed a soundcloud playlist in pages, posts, blogs, etc use the following:
    `< soundcloud/playlist ########## >`

     where `##########` is the numeric playlist ID — the value SoundCloud uses
     in its API playlist URL, not the playlist slug.
